# Changesets

Release notes live here. Run `pnpm changeset` to record a change; the release
workflow versions and publishes via the "Version Packages" PR on `main`.

Docs: https://github.com/changesets/changesets
